package com.project.fitnessbuddy.database.entity.enums

import com.project.fitnessbuddy.R

enum class Frequency(val resourceId: Int) {
    DAILY(R.string.daily),
    WEEKLY(R.string.weekly),
    MONTHLY(R.string.monthly),
}
